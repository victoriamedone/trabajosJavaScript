const prompt = require('prompt-sync')();

// Tienes una lista de colores ["Rojo", "Verde", "Azul", "Amarillo"]. Encuentra y
// muestra el índice del color "Azul" en la lista.


let colores = ["Rojo", "Verde", "Azul", "Amarillo"]; 

let indiceEncontrado = colores.indexOf("Azul"); 

console.log("El color azul se encuentra en la posicion nro:", indiceEncontrado); 